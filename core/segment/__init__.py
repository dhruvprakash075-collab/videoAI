"""core.segment package — Per-segment pipeline graph nodes and utilities.

Submodules:
    budget: _tts_word_budget, _trim_script_to_word_limit
    identity: _detect_important_trigger, _perceptual_hash
    retry: build_retry_wrapper
    graph: SegmentGraphBuilder, SegmentState (LangGraph state machine)
"""
