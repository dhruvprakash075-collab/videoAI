# GitHub Issues — Studio TUI v2

Generated by the `plan-to-issues` skill. Create these issues in order.
If `gh` CLI is available: `gh issue create --title "..." --label "..." --body "..."`

## File Overlap Matrix (Iron Rule: same file = different waves)

| File | Tasks that touch it |
|---|---|
| `studio_tui.py` | 1,2,5,6,7,8,9,10,11,13,14,16,17,18,19,22,23,24 |
| `core/pipeline_long.py` | 0,4,15,21 |
| `agents/director_agent.py` | 0,3 |
| `studio_tui_helpers.py` | 3.1,14,23 |
| `utils/local_ui.py` | 0 |
| `tests/conftest.py` | 3.1 |
| `tests/test_uistate.py` | 3.1 |

**Conclusion:** `studio_tui.py` tasks must be sequential (one at a time).
`pipeline_long.py` tasks must be sequential. All other files are touched by ≤1 task.

## Wave Execution Plan

| Wave | Issues | Parallel? | Depends on |
|---|---|---|---|
| W1 | #0 (pre-flight fixes) | N/A | — |
| W2 | #1 (theme), #3 (UIState) | ✅ Yes | W1 |
| W3 | #2 (tabs), #3.1 (tests), #4 (hooks) | ✅ Yes | W2 |
| W4 | #5 (progress), #6 (status), #7 (stats), #10 (min-size), #11 (bell) | ❌ No — all touch studio_tui.py | W3 |
| W5 | #8 (poll), #9 (keys) | ❌ No — studio_tui.py | W4 |
| W6 | #12 (P1 gate) | N/A | W5 |
| W7 | #13 (options), #15 (cancel hook) | ✅ Yes — different files | W6 |
| W8 | #14 (file source), #16 (pause/cancel) | ❌ No — studio_tui.py | W7 |
| W9 | #17 (output), #18 (QoL), #19 (a11y) | ❌ No — studio_tui.py | W8 |
| W10 | #20 (P2 gate) | N/A | W9 |
| W11 | #21 (preflight collector) | N/A | W10 |
| W12 | #22 (preflight panel), #23 (checkpoints), #24 (artifacts) | ❌ No — studio_tui.py | W11 |
| W13 | #25 (P3 gate) | N/A | W12 |

---

## Issue Bodies

### Issue #0 — Pre-flight: fix 3 live bugs

**Labels:** `P0`, `wave-1`, `fix`, `tui`

```
## Summary
Fix three live bugs before the TUI rewrite begins. These affect the current
production code regardless of the TUI enhancement work.

## Files to Create/Modify
- [ ] `utils/local_ui.py` — stop logging user_reply verbatim (line 330)
- [ ] `agents/director_agent.py` — change pause_event default to threading.Event()
- [ ] `core/pipeline_long.py` — reset _director_abort at start of run_long_pipeline()

## Acceptance Criteria
- [ ] `utils/local_ui.py` line 330 logs only the reply length, not the text
- [ ] `UIState.pause_event` class default is `threading.Event()`, not `None`
- [ ] `run_long_pipeline()` calls `_director_set_abort(False)` as its first statement
- [ ] `py_compile` passes on all three files
- [ ] A second run after a cancel does NOT silently skip all segments

## Dependencies
- Blocks: all other issues (must be done first)

## Wave Assignment
**Wave 1** — foundational fixes, no dependencies
```

---

### Issue #1 — Fix Header/Footer render bug + theme

**Labels:** `P0`, `wave-2`, `feat`, `tui`

```
## Summary
Remove the border from Header and Footer CSS (root cause of empty-box bug).
Register a centralized studio-dark Theme.

## Files to Create/Modify
- [ ] `studio_tui.py` — remove `border: round #30363d` from Header and Footer CSS;
  add `App.register_theme()` + `self.theme = "studio-dark"`

## Acceptance Criteria
- [ ] PTY launch: Header shows title/clock, Footer shows keybindings (not empty boxes)
- [ ] All log-line styles use hex colors (e.g. `#58a6ff`), never CSS class names
- [ ] `py_compile studio_tui.py` passes

## Dependencies
- Depends on: #0
- Blocks: #2, #5, #6, #7, #8, #9, #10, #11

## Wave Assignment
**Wave 2** — parallel with #3 (different files)
Can run in parallel with: #3
```

---

### Issue #3 — Add additive UIState progress fields

**Labels:** `P0`, `wave-2`, `feat`, `tui`

```
## Summary
Add segment_current, segment_total, run_start_ts, vram_text fields and
reset_run() / set_progress() classmethods to UIState. Additive only.

## Files to Create/Modify
- [ ] `agents/director_agent.py` — add 4 class attrs + 2 classmethods

## Acceptance Criteria
- [ ] `UIState.set_progress(total=12)` sets segment_total=12, segment_current unchanged
- [ ] `UIState.reset_run("topic")` zeroes counters, sets run_start_ts within 1s of now
- [ ] All existing UIState attrs/methods unchanged
- [ ] `py_compile agents/director_agent.py` passes

## Dependencies
- Depends on: #0
- Blocks: #3.1, #4

## Wave Assignment
**Wave 2** — parallel with #1 (different files)
Can run in parallel with: #1
```

---

### Issue #2 — Tab skeleton (Run / Stats / Help)

**Labels:** `P0`, `wave-3`, `feat`, `tui`

```
## Summary
Replace the single Vertical body with TabbedContent holding Run/Stats/Help panes.
Run pane: status panel + progress bar placeholder + RichLog + Input.

## Files to Create/Modify
- [ ] `studio_tui.py` — replace compose() body with TabbedContent + 3 TabPanes

## Acceptance Criteria
- [ ] PTY: three tabs visible; F1/F2/F3 switch between them
- [ ] Run tab is default on launch
- [ ] Switching tabs does not reset log_index or disrupt the log
- [ ] `py_compile studio_tui.py` passes

## Dependencies
- Depends on: #1
- Blocks: #5, #6, #7, #8, #9, #10, #11

## Wave Assignment
**Wave 3** — parallel with #3.1 and #4 (different files)
Can run in parallel with: #3.1, #4
```

---

### Issue #3.1 — UIState unit tests + extract pure helpers

**Labels:** `P0`, `wave-3`, `test`, `tui`

```
## Summary
Write unit tests for UIState helpers. Extract pure helper functions into
studio_tui_helpers.py. Add conftest.py with autouse UIState reset fixture.

## Files to Create/Modify
- [ ] `tests/conftest.py` — autouse fixture resetting ALL UIState fields
- [ ] `tests/test_uistate.py` — 15+ test cases (see builder-guide.md §6)
- [ ] `studio_tui_helpers.py` — _format_elapsed, _format_etc, _parse_duration

## Acceptance Criteria
- [ ] `pytest tests/test_uistate.py -v` all green
- [ ] conftest.py resets ALL UIState fields (not just the 4 new ones)
- [ ] _format_elapsed(0) returns "—"; _format_etc(0, 12, ts) returns "—"
- [ ] _parse_duration("99999") returns None (upper bound 480)
- [ ] Tests are isolated — order-independent

## Dependencies
- Depends on: #3
- Blocks: #14, #23 (studio_tui_helpers.py)

## Wave Assignment
**Wave 3** — parallel with #2 and #4 (different files)
Can run in parallel with: #2, #4
```

---

### Issue #4 — Wire pipeline progress hooks

**Labels:** `P0`, `wave-3`, `feat`, `tui`

```
## Summary
Add 3 additive writes to pipeline_long.py: set total after outline reconciliation,
set current inside the existing completed_segs_lock, extend _log_vram_usage.

## Files to Create/Modify
- [ ] `core/pipeline_long.py` — 3 additive writes (see builder-guide.md §1)

## Acceptance Criteria
- [ ] `UIState.set_progress(total=n_segs)` called after outline reconciliation
- [ ] `UIState.set_progress(current=completed_segs)` called INSIDE completed_segs_lock
- [ ] `UIState.vram_text` set by `_log_vram_usage()`
- [ ] `--dry-run` non-TUI run behavior unchanged
- [ ] `py_compile core/pipeline_long.py` passes

## Dependencies
- Depends on: #3
- Blocks: #15, #21

## Wave Assignment
**Wave 3** — parallel with #2 and #3.1 (different files)
Can run in parallel with: #2, #3.1
```

---

### Issues #5–#11 — Phase 1 widget tasks (sequential, all touch studio_tui.py)

**Labels:** `P1`, `wave-4` or `wave-5`, `feat`, `tui`

These must be done one at a time (all touch `studio_tui.py`):

| # | Title | Wave | Depends on |
|---|---|---|---|
| 5 | Progress bar (planning + 100% states) | 4 | #2, #3 |
| 6 | Status panel + ETC (badge + meta line) | 4 | #2, #3 |
| 7 | Stats 2×2 grid + VRAM gauge + sparkline | 4 | #2, #3 |
| 10 | Min-size guard + crash log hardening | 4 | #1 |
| 11 | Completion bell (fires once per transition) | 4 | #3 |
| 8 | Poll hardening + log-index reset + _last_question fix | 5 | #5,#6,#7 |
| 9 | Keybindings + footer (F1/F2/F3/Ctrl+E/Ctrl+L) | 5 | #8 |

---

### Issue #12 — Phase 1 verification gate

**Labels:** `P0`, `wave-6`, `test`, `tui`

```
## Summary
Verify Phase 1 is complete and nothing regressed before starting Phase 2.

## Files to Create/Modify
- [ ] `tests/test_phase1.py` — PTY-driven verification scenarios

## Acceptance Criteria
- [ ] py_compile passes on all touched files
- [ ] FastAPI TestClient: /api/status returns exactly {status, active_question, logs, output_video}
- [ ] PTY: Header/Footer non-empty, tabs work, progress bar shows planning then advances
- [ ] PTY: 2nd run shows early logs (log_index reset working)
- [ ] PTY: pause prompt stays visible until reply typed (_last_question fix working)
- [ ] PTY: bell fires once on complete; min-size notice shows on small terminal

## Dependencies
- Depends on: #5, #6, #7, #8, #9, #10, #11
- Blocks: all Phase 2 issues

## Wave Assignment
**Wave 6** — Phase 1 gate
```

---

### Issues #13–#19 — Phase 2 run control (sequential within studio_tui.py)

| # | Title | Wave | Parallel with | Depends on |
|---|---|---|---|---|
| 13 | Options form (duration/resume/RVC/director/preview) | 7 | #15 | #12 |
| 15 | request_cancel() hook in pipeline_long.py | 7 | #13 | #12 |
| 14 | File source input + containment check | 8 | — | #13, #3.1 |
| 16 | Pause/cancel UI (F5 + Ctrl+X + confirm modal) | 8 | — | #15 |
| 17 | Output access + run history (Ctrl+O) | 9 | — | #14, #16 |
| 18 | QoL: palette, toasts, copy, confirm-quit, session log | 9 | — | #17 |
| 19 | Accessibility: focus order, Escape modals, resilience | 9 | — | #18 |

**Key security requirements per issue:**
- #13: duration upper bound 480; project name through `_safe_filename()`
- #14: `Path.resolve()` + `is_relative_to(project_root)` containment check
- #18: session log filename uses `strftime('%Y%m%d_%H%M%S')` not `isoformat()`
- #23: checkpoint topic through `_safe_checkpoint_topic()` before `clear()`

---

### Issue #20 — Phase 2 verification gate

**Labels:** `P0`, `wave-10`, `test`, `tui`

```
## Acceptance Criteria
- [ ] PTY: start with options, file source, pause/resume, cancel-keeps-checkpoints
- [ ] PTY: pause→cancel sequence (no deadlock)
- [ ] PTY: min-size recovery (shrink then enlarge)
- [ ] Security: ../../config/config.yaml as file input is rejected
- [ ] Security: duration 99999 is rejected
- [ ] Security: session log filename has no colons
- [ ] No Phase 1 regressions

## Dependencies
- Depends on: #13, #14, #15, #16, #17, #18, #19
- Blocks: all Phase 3 issues
```

---

### Issues #21–#24 — Phase 3 inspection panels

| # | Title | Wave | Parallel with | Depends on |
|---|---|---|---|---|
| 21 | Extract preflight collector (optional refactor) | 11 | — | #20 |
| 22 | Preflight health panel (F6 re-run) | 12 | — | #21 |
| 23 | Checkpoints screen (list + confirm-clear) | 12 | — | #21, #3.1 |
| 24 | Artifacts viewer (manifest + segments + chapters) | 12 | — | #21 |

Note: #22, #23, #24 all touch `studio_tui.py` so they must be sequential despite being in the same wave conceptually. Assign them to waves 12a, 12b, 12c.

---

### Issue #25 — Phase 3 verification gate + docs

**Labels:** `P0`, `wave-13`, `test`, `tui`

```
## Acceptance Criteria
- [ ] PTY: preflight panel renders + re-run works
- [ ] PTY: checkpoint list + confirmed clear (crafted ../config stem rejected)
- [ ] PTY: artifact viewer handles missing/malformed files gracefully
- [ ] Help pane updated with all screens and keybindings
- [ ] launch_tui.ps1 reference updated
- [ ] No Phase 1 or Phase 2 regressions

## Dependencies
- Depends on: #22, #23, #24
```

---

## Quick-create commands (once `gh` is installed)

```powershell
# Install gh: winget install --id GitHub.cli
# Then authenticate: gh auth login

# Create labels first
gh label create "P0" --color "FF0000" --description "Critical"
gh label create "P1" --color "FF6B6B" --description "High"
gh label create "wave-1" --color "4ECDC4"
gh label create "wave-2" --color "45B7D1"
gh label create "tui" --color "7B68EE" --description "Studio TUI"

# Then create issues from this file's bodies
```
