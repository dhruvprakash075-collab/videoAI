# Session 2026-08-05 — Storyboard hardening pass

## Context loaded
Session-memory digest of 2026-08-04: storyboard phase (Storyboard-Prompt-Builder)
implemented end-to-end (commits 4a123bb63, b9a8759ce, 76a5db188, all pushed to
`github-origin`, never `origin`). Static bug-hunt closed gaps; CRITICAL finding
outstanding: dry-run ran the full storyboard gate.

## Decisions (user-locked this session)
- "fix all 4": dry-run gate, hook wiring test, regenerate feedback, dead `aspect` config.

## Work done
1. **Dry-run gate (MAJOR)** — `core/pipeline_long.py` hook now `if not (dry_run or fast_dry_run)`.
   Dry runs previously did real LLM + ComfyUI generations, composed the sheet, and
   PERSISTED an approved record that the real run would silently reuse
   (`finalize_dry_run` exits after the hook, so it always ran).
2. **Hook wiring extracted** — `wire_storyboard(config, outline, storyboard)` in
   `core/storyboard.py`: sets `storyboard.approved_sheet`/`panels`,
   `image_gen.comfyui.storyboard_sheet`, rides shot metadata onto outline segments.
   Idempotent, no-op on None.
3. **Regenerate uses feedback** — on Regenerate, `consult_user("What should change...",
   allow_custom=True)`; real feedback appended as `USER FEEDBACK (...)` to next
   attempt's prompt; unattended defaults ("Proceed as planned." / "Proceed with
   default settings.") filtered; prior feedback persists across retries.
4. **Dead config removed** — `storyboard.aspect` deleted from `StoryboardConfig`
   (config_schemas.py) + config.yaml; fallback page is fixed 16:9 constant;
   `_page_aspect_from_config` deleted.

## Tests
- New: `test_storyboard_gate_skipped_on_dry_run` (regression guard), 2x wire_storyboard,
  2x regenerate feedback; `_FakeDirector` now distinguishes feedback consults
  (options=None → feedback_choices).
- 78 targeted + 1937 full suite (excl. node tests) + 71 node tests: all pass.
- `ruff check .` clean.

## Commits
- `18245df6e` fix: storyboard dry-run gate, regenerate feedback, wiring test, dead config

## Real-instance smoke (user asked to test the storyboard as intended)
- Driver ran the REAL path: hermes-director plan → 6 ComfyUI panels → 2 composed
  sheets → approval → StoryStore persist (212s). Run 2 reused the record in 0.0s.
- Found + fixed real bug: str layout_file values crashed the compositor
  (`'str' object has no attribute 'is_file'`) — `Path()` coercion added in
  `_layout_rects`/`_read_layout_counts` (shared-function fix). Regression test
  `test_compose_accepts_str_layout_file_values`. Commit `8ab64e957`.
- Ollama cold-load flakiness (hermes-director → WinError 10054): clean restart +
  warm-up fixed it. Model loads take ~40s on this box.
- Full suite 1938 passed/5 skipped, ruff clean. Sheets verified 1920x1080,
  real content (model can't render images — PIL pixel-variance check instead).

## Environment
- Ollama running (`ollama serve`): cra-guided-7b, zephyr-writer, hermes-director,
  sarvam-translate, qwen2.5:0.5b. ComfyUI 0.24.0 at 127.0.0.1:8188 (RTX 4050 6GB).
- No real-instance smoke run (user forbade real runs). `scripts/comfyui_smoke.py`
  is the pending gate for next time.

## Open / next
- Real-instance smoke run when user allows (validates executor-level parse of
  injected `approved_sheet`/`panels`).

---

# Session 2026-08-05 (evening) — Test-intention audit

## What was done
- Audited whether tests fulfill their intention (not just pass): subagent tautology scan + direct verification of every HIGH finding.
- 2006 tests passed but 16 false-assurance findings; fixed the 9 HIGH + 3 MEDIUM:
  - DELETED (guard ghosts / duplicate real coverage): test_word_trim.py (production W4 trim never existed), test_seed_resolution.py (seed_map builder absent from codebase), test_retry_budget.py (build_retry_wrapper already covered in test_segment_runner_helpers.py), test_thumbnail.py (_generate_thumbnail already covered in test_post_production.py)
  - REMOVED 3 tautological forwarder tests (test_pipeline_long.py:29-45) that patched the real set_director_abort out
  - REWROTE test_story_cache.py → exercises real StoryMixin.invent_story cache (sha256[:12] keying, hit, force_refresh, config-disable, case-insensitive key) via _FakeDirector
  - REWROTE test_batch_mode.py → real _load_topics_file + _run_batch (extracted from bootstrap_pipeline.py main()) with fake pipeline, continue-on-failure, report structure
  - REWROTE test_bootstrap_sentry_smoke.py → drives real run_pipeline_with_args branch via sys.argv (exit 0 and exit 1 paths)
  - test_bootstrap_cli_accepts_float → real _build_parser() instead of argparse replication
  - test_float_safe_ceil_segment_count → real _ceil_segments helper (extracted core/pipeline_long.py:224 call site)
  - TestWriterMaxTokensAutoScale → real _auto_max_tokens helper (extracted core/segment_runner.py:329 call site)
  - Added completion assertions to test_translate_node_falls_back_on_crash + test_translate_node_bloated_translation_falls_back
- Verification: 1986 passed / 3 skipped, ruff clean, mypy clean (CI cmd), coverage 90.6% >= 80% gate.

## Decisions
- Deletion over rewrite: duplicate/ghost tests removed instead of rewired (real coverage already existed elsewhere).
- Extract-once helpers (_ceil_segments, _auto_max_tokens, _load_topics_file) made inline production formulas testable without duplicating them in tests.

## Commits
- (code changes staged locally, not committed — user review pending)

## Open / next
- LEFT as-is (weak but real guards, calls production): test_omnivoice_worker._set_seed no-op, test_sentry env assertions.
- ComfyUI real-instance smoke (`scripts/comfyui_smoke.py`) still pending.
