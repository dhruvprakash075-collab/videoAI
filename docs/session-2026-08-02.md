# Session Log — 2026-08-02: Debt-Elimination Sprint (WS-1..WS-5)

Context for future sessions. Covers the full multi-agent sprint, all commits,
decisions, and open items.

## Sprint scope (plan)

5 workstreams, 5 agents, all merged to `main` and pushed to GitHub:

| WS | Work | Result |
|---|---|---|
| WS-1 | `core/pipeline_long.py` decomposition | 5 module-level helpers extracted: `_assemble_cli_flags`, `_resolve_decision_record` (returns 5-tuple incl. `rec_total_duration_min`; caller does the `config["video"]` write), `_adjust_outline_length`, `_run_phase`/`_run_staged_batches` (DRYs the 5 identical evict→try→log.error blocks), `_run_parallel_segments`. Stale `make_process_segment` docstring fixed (6-tuple, `segment_runner.py:1098`). |
| WS-2 | Degradation callback dedupe | Real bug: `run_long_pipeline` re-registered `UIState.add_degradation` per run; nothing ever cleared the registry → 2nd run doubled every degradation (~12 call sites). Fix: identity dedupe in `audio/__init__.py:add_degradation_callback`. |
| WS-3 | Config schema completion | Added `negative_prompt` to `VisualConfig` (shipped `series_1.yaml` was REJECTED by old schema — live bug). Deleted dead `VideoAIConfig` (~60 lines, 0 importers). All direct `config["x"]` reads → `.get()`/`setdefault` (post_production, assembler, pipeline_long). Runtime overlays (`provenance`, `_director_vision`, `_decision_record`) correctly excluded — pipeline-injected, not user config. |
| WS-4 | DirectorAgent mixin split | `agents/director_agent.py` 2408→99 lines thin facade + 8 mixins in `agents/director/`: consultation(456), story(375), vision(336), config_production(898), translation(210), memory_sync(84), prompts(89), llm_shims(54). MRO: `Consultation → Vision → ConfigProduction → Story → Translation → MemorySync → LlmShims → Prompts` (helper mixins rightmost so `_prompt`/`_parse_json` can't be shadowed). Re-exports preserved (`DirectorAgent`, `UIState`, `_devanagari_ratio`, `extract_json`, hinglish helpers). |
| WS-5 | Verification gate | Full suite + ruff + mypy + ComfyUI smoke gate (user-run). |

## Post-sprint defect fixes (audited)

1. **`@functools.lru_cache(maxsize=2)` on `load_config`** — cached the *returned mutable dict*; run-1 in-place mutations (e.g. `--duration 30`) leaked into run 2 in-process. Fix: **dropped the cache** (audio_proxy already caches at its own layer; shared mutable dict across TUI/FastAPI/pipeline threads was the hazard). Regression test `test_load_config_returns_fresh_dict_per_call`.
2. **Supertonic temp file leak** (`audio_proxy.py` one-shot fallback) — `supertonic_input_*.txt` never unlinked. Fix: `finally` + `contextlib.suppress(OSError)` + `temp_file.unlink()`, matching the IndicF5 pattern (audio_proxy.py:173-175). Covers `TimeoutExpired` path.
3. **`_input` bridge in `consultation.py`** — sys.modules magic reading the facade namespace was needed only because 8 tests patched `agents.director_agent.input` (vestigial monolith-era target). Fix: migrated those 8 sites to `patch("builtins.input")` (same style as the other 11) and deleted the bridge.

## Commits (oldest→newest, all pushed)

- `2ac702d97` fix: dedupe degradation callbacks on restart, document UIState registry contract
- `f07400cb9` refactor: extract pipeline_long helpers, fix make_process_segment docstring
- `acedc9676` feat: complete config schema validation, drop load_config cache, remove dead VideoAIConfig
- `49e4f4f58` chore: commit in-flight work (preflight refactor, supertonic/omnivoice workers, ollama runtime, misc fixes) — 14 files of pre-existing uncommitted work from an earlier session
- `da042562f` refactor: split DirectorAgent into 8 role mixins behind a thin facade (WS-4)
- `31fa72bf1` fix: drop legacy INPUT_TYPES overrides that crash V3 execution on combo inputs
- `f56c01827` test: gate executor-format drift + real-instance smoke script; pin vendored ComfyUI (user commit)
- `da5778a8e` refactor: drop _input bridge, patch builtins.input in consultation tests
- `ae150cfae` chore: cleanup — lock reset_run, fix docstring, add ponytail debt ledger

## Upstream (not ours, pulled)

Copilot PRs on GitHub: CI job fixes (#62, #63) and preflight dry-run handling in `core/pipeline_long.py` (`94cec06a7`, `c0a1a2b92` — merged via `aec49c199`). Also `8d07393b9` (older): "Remove Qwen image edit system" — contains the preflight tests.

## Environment gotchas

- **Remote is `github-origin`, NOT `origin`**: `https://github.com/dhruvprakash075-collab/videoAI.git`. `git fetch origin` fails; always use `github-origin`. Push: `git push github-origin main`.
- Python interp: `python` = repo `venv` (torch 2.12.0+cu128). ComfyUI has its own `.venv`.
- `mypy` not on PATH — use `python -m mypy --follow-imports=skip --ignore-missing-imports <file>`.
- Test count baseline: **2040 passed, 5 skipped** (smoke auto-skip), ruff clean, full suite ~2 min.
- `config/comfyui/vendored_comfyui.txt` pins the vendored clone (`2cdaaf4a`); update whenever `external/comfyui` refreshes. No automation enforces it — the regression test `test_input_types_override_format_compatible_with_executor` catches executor drift in CI without GPU.

## Cleanup (session end)

- `agents/ui_state.py:reset_run` now resets class attrs under `_log_lock` (was racy vs `add_degradation`).
- `audio/__init__.py` docstring fixed (`set_degradation_callback` → `add_degradation_callback`).
- `PONYTAIL-DEBT.md` — 31 `ponytail:` markers harvested; **3 no-trigger rows** (silent-rot risk): `hinglish_glossary.py:365`, `indicf5_worker.py:39`, `pipeline_long.py:36`.
- Deleted gitignored junk: studio_checkpoints/studio_outputs artifacts, logs, caches, 2227 `__pycache__` dirs, `GITHUB_SETUP_INSTRUCTIONS.md` (obsolete). Kept `Modelfile.*` (Ollama persona defs — only record of the local director/writer model config).
- Secrets scan on tracked tree: clean.

## Open items

1. **ComfyUI smoke gate** is a manual step: `scripts/comfyui_smoke.py` on a live instance after any schema/ComfyUI change (AGENTS.md mandates it). User runs it.
2. **Pin-drift CI check** (diff `external/comfyui` HEAD vs `vendored_comfyui.txt`) — offered as a 5-line CI step, not added (user declined for now).
3. **Preflight rewrite** (`core/preflight.py` → PreflightResult/PreflightCheck from `utils.preflight`) — committed as in-flight work; tests exist (`test_preflight*.py`, tracked in `8d07393b9`) and pass; behavior claims verified by suite only.
4. **UIState `warning_count` vs `degradations` ledger** — duplicate source of truth smell; documented, not refactored.
5. Tier-4 full dead-code/yagni sweep — deliberately skipped (sprint already cut the big items).

## Key decisions to respect

- **Do not bump pinned deps** (torch, ComfyUI ecosystem) without full suite + stub compatibility check (AGENTS.md).
- **Do not touch** `bootstrap_pipeline.py`, `core/` (except sanctioned), `video/`, or SQLite schema (Rust crate rules).
- `ponytail:` comments = intentional shortcuts with named ceilings; harvest to `PONYTAIL-DEBT.md`, don't silently "fix" them.
