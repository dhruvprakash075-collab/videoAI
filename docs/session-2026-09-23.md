# Session Log — 2026-09-23: full health check, recycle-bin recovery, working-tree cleanup

## What was done
- Full verification green: `ruff check .` clean; `mypy agents/ui_state.py` clean; `cargo test` (79 tests) + `clippy -D warnings` + `fmt --check` clean; pytest **1957 passed / 5 skipped**, coverage **91.0% ≥ 80**.
- Recycle-bin audit: only real project loss was `scratch/generate_audio_only.py` (audio-only story→TTS shortcut) — **restored** to `scratch/`, syntax-checked. 3 planning docs were moved (not lost): root copies byte-identical to `HEAD:docs/*`. Everything else was cargo artifacts, git locks, SQLite -wal/shm, verify-worktree metadata. Flagged unrelated `D:\android-sdk|avd|user-home` (~17GB) deleted 09:12.
- Working-tree cleanup: docs moved back to `docs/` via `git restore` + root copies removed; `.workbuddy-ai/` gitignored (agent state + binary probe); committed `scratch/generate_audio_only.py` + `.gitignore` + `rust/Cargo.lock` (377 crate adds / 52 removes — resolution churn from cargo runs).

## Commits
- `<hash>` docs+chore: restore planning docs, recover scratch script, ignore .workbuddy-ai, lockfile sync (this session)

## Decisions
- Restore docs in place (`git restore`) rather than commit the move — content identical, cleaner history.
- Gitignore `.workbuddy-ai/` — another tool's state + `probe/voice.wav` binary, not project source.
- Commit `rust/Cargo.lock` churn as-is — pure cargo resolution update, no Cargo.toml change.

## Gotchas
- Remote is `github-origin`, never `origin`.
- `rg` not on PATH in pwsh; `mypy` not on PATH — use `.\venv\Scripts\python.exe -m mypy`.
- Recycle-bin source-file scan must use Shell COM (`Namespace(0xA)`) for original paths — `$R*` data files alone don't say where they came from.
- Coverage gaps worth knowing: `core/pipeline_cli.py` 52.6%, `utils/sentry.py` 56%, `agents/director/vision.py` 56.2%.
- Cargo bin/lib pdb filename collision warning (`videoai-worker` vs `videoai_worker`) — may become a hard error (cargo#6313).

## Open items
- 3 `no-trigger` ponytail debt rows still rotten: `hinglish_glossary.py:365`, `indicf5_worker.py:39`, `pipeline_long.py:36`.
- Unrelated: `D:\android-sdk` + `android-avd` + `android-user-home` in recycle bin — restore if unintentional.
- ComfyUI real-instance smoke (`scripts/comfyui_smoke.py`) not run this session.
- `stash@{0}` "dirty feature work" on `chore/requirements-bonsai-cleanup` untouched.
