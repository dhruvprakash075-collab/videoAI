# Session Log — 2026-08-03: Audit-fix commit + push (deliverable closed)

Context for future sessions. Covers the final commit + push of the full audit
fix set (Phases 1-2 + items 3/4/5/7/8/9 + deep-clean).

## What was done
- Verified remote is `github-origin` (`https://github.com/dhruvprakash075-collab/videoAI.git`), pushed commit `6b4b8a907` → `main` (`4cce561df..6b4b8a907`).
- Verified final tree state: clean except `?? tests/test_outline_shaping.py` (intentionally untracked — user saw the flag, chose "ask first" not "commit it").

## Commits
- `6b4b8a907` fix: execute audit findings (Phases 1-2) + tier-4 dead-code sweep + deep-clean (182 files, +901 −7248; staged via `git add -u` so the untracked test stayed out; no secrets in staged names)

## Decisions
- `test_outline_shaping.py` stays untracked — tests `core/outline_shaping.py` `shape_outline`, 2 tests pass, no other coverage; commit only if user explicitly asks.
- Everything else in the audit-fix deliverable (bugs.md #1-43, items 3/4/5/7/8/9, deep-clean groups A+B) is now committed and pushed.

## Gotchas
- Remote is `github-origin`, NOT `origin` (AGENTS.md). Push: `git push github-origin HEAD`.
- Final suite state: **1942 passed / 5 skipped**, ruff clean, compileall 0, cargo test/clippy/fmt clean, config load + schema valid.

## Open items
- `tests/test_outline_shaping.py` — decide commit (recommended: commit; it's the only coverage for `shape_outline`).
- ComfyUI smoke gate remains a manual step after any ComfyUI/schema change (`venv\Scripts\python.exe scripts\comfyui_smoke.py`).
- Real-run verification of advisory-`--duration` + word-count gate (optional ~15 min).

---

# Session Log — 2026-08-03 (evening): IndicF5 TTS restore + image-pass split (Part A + B DONE)

Second session today. Plan `docs/plans/2026-08-03-tts-and-image-pass-plan.md` — Part A (IndicF5) and Part B (refine-pass split) both delivered, measured, committed, pushed.

## What was done
- Part A: gated-repo 401 diagnosis superseded — real blockers were transformers 5.x meta-device init, `torch.compile`/`_thread_safe_fork`, and `load_model` ckpt signature; fixed operationally in `external/IndicF5/run_indic.py:_load_model`. `config/config.yaml:20` `engine: supertonic` → `indicf5` (undoes 08-02 workaround flip). HF_TOKEN + HF_HOME persisted as User env vars.
- Part B code: `config/comfyui/workflows/manga_face_detail_api.json` + `manga_upscale_api.json` (split from refine_upscale); `ComfyUIConfig.face_detail/upscale` + `refine_upscale` back-compat alias in `config/config_schemas.py`; `image_gen.py:_refine_passes` chains enabled passes; tests parametrized; 83 passed.
- B5 measurement: 3 × 1-seg real runs all SUCCESS — face-only 51.7s video, upscale-only 118.9s, both 59.4s. Upscale pass ≈ 3 min/img (incl. warm-up), face detail ≈ 15-20 s/img. Defaults: both passes on.
- Incident: intermittent Windows child-spawn failures rc=0xC0000142 (12ms, empty stdout) inside batch chains only (TTS worker, one-shots, even trivial helper spawn); ~2/7 batches. Not reproducible (40× loop OK), no event-log entries, cwd/env/PATH/temp verified clean. Fix: `audio/audio_proxy.py:_run_worker` — retry once on 0xC0000142 at 3s/10s at all three one-shot spawn sites (indicf5/supertonic/omnivoice). Both post-fix runs spawned first-try.
- Killed 29h zombie ComfyUI duplicate (PID 7060, 1MB WS hung) — it took the real server (PID 14400) down with it (same job tree); relaunched ComfyUI from `external/comfyui` (PID 24940, port 8188, 4.96GB VRAM free).

## Commits
- `73d380539` feat: split refine pass into face_detail/upscale; retry TTS worker spawns on Windows 0xC0000142 (10 files, +400 −87)

## Decisions
- Default image passes = both on (`face_detail: true, upscale: true`, `refine_upscale: false` — alias equivalent, run 3 proved both execute). Upscale-cost reduction (2.5×→2.0×, steps 12→10) deferred — workflow cost driver, not plumbing.
- Debug probe (VIDEOAI_SPAWN_DEBUG) removed after diagnosis; retry helper kept as the real fix (logs retries).
- ComfyUI stays running at PID 24940 for future runs; pipeline will adopt/restart it via `ensure_running` if needed.

## Gotchas
- Remote is `github-origin`; push `git push github-origin HEAD`.
- Always `venv\Scripts\python.exe -X utf8` for Devanagari (cp1252 UnicodeEncodeError otherwise).
- Killing a "zombie" process can take down its whole job tree — verify port owner (`netstat -ano`) before/after.
- `set_passes.py` helper (Temp\opencode) sets `refine_upscale: false` + explicit face_detail/upscale flags; `1 1` = both.

## Open items
- Watch for 0xC0000142 recurrence; `_run_worker` retry log line `[audio_proxy] worker spawn rc=0xC0000142 on attempt N` is the tripwire. If it recurs with the retry, capture one with `VIDEOAI_SPAWN_DEBUG` probe re-added.
- ComfyUI smoke gate after next ComfyUI update: `venv\Scripts\python.exe scripts\comfyui_smoke.py`.
- Optional: upscale workflow cost tuning (2.5×→2.0× / steps 12→10) per B5 data.
- Character consistency across panels (protagonist ≈ 4 different people) — separate task, per plan.
