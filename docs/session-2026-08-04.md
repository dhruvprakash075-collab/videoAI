# Session Log — 2026-08-04: user verdict on Lantern Maker run + voice artifact fix

Context for future sessions. Covers the user's verdict on the 117.5s
The_Lantern_Maker_of_Old_Delhi run, the root-cause investigation of both
problems, the applied IndicF5 voice fix (verified), and the HELD image-count
topic with the exact re-run path.

## Verdict (from user)
- Page layout: PERFECT — keep as-is.
- Problem 1: "story dont match photo". Problem 2: voice artifact "like spoken by narrator".
- User locked: fix BOTH, but DO NOT change the reference voice
  (`character_voices/narration_ref_9s_mono24k.txt` + ref audio) — "it is perfect voice".

## Problem 1 — story vs photo (root cause, fix HELD)
- The narration DOES match the SRT subtitles (Azhar/Zahra story, verified via
  `studio_checkpoints/story_memory.json` + narration text). The mismatch is
  timing: the CLI locked 1 segment / 130 words / 3 images
  (`studio_checkpoints/config_overlay_The_Lantern_Maker_of_Old_Delhi.json`
  `_decision_record`: segment_count=1, words_per_segment=130, images_per_segment=3
  [writer, unlocked]) → 6 scene images → 1 composed page → the whole 117.5s
  video is one A4 page with zoompan. PSNR 21-23dB across 5s/30s/60s/90s frames
  = motion on same content.
- Mechanism: `bootstrap_pipeline.py:273` `--images-per-segment` locks the
  DecisionRecord → `config_schemas.py:814` overlay `default_images_per_segment`
  → `core/outline_shaping.py:74-83` forces `num_images` on every segment.
  `compose_panel_pages` pages 5 panels/page; `assembler._build_image_slideshow_cmd`
  splits total duration evenly across the composed pages.
- Options presented (user: "hold this topic for now"):
  A) panels as video frames — 25 scene images (~1/sentence), crossfade ~4.7s each;
     pages kept as poster/thumbnail. ~1.5h upscale-on / ~15 min off.
  B) 5 pages of 5 panels, page flips ~24s (full-page look kept). ~1.5h.
  C) 3 pages / 15 images (~39s/page). ~1h.
- Re-run command shape for next time: add `--images-per-segment 25` (or 15)
  to the usual generate-video CLI line; TTS re-run is automatic (~20 min).
- Panel crops ≈ the scene images themselves (generated at panel aspect in story
  order, `video/image_gen/image_gen.py:214-240`) — option A needs no compositor
  change, just routing scene images to the slideshow instead of pages.

## Problem 2 — voice artifact (ROOT CAUSE + FIX DONE, verified)
- Not whisper-tiny word-doubling (that was tiny's hallucination; faster-whisper
  `small` reads the text clean). Real artifacts: "गई/गय" filler at sentence
  boundaries, occasional stutter ("अज़हर अज़हर"), mispronunciations, monotone.
- Mechanism: F5 duration model overpredicts frame count; speed 0.9 inflates the
  budget ~11%; the model fills surplus frames with a last-phoneme drawl whose
  head sounds like "गई"; `_tail_trim` at pad 1.05 cut into the drawl but left
  the crisp "गई" head.
- A/B matrix (all in `C:\Users\dhruv\AppData\Local\Temp\opencode\`, whisper-small
  verified): ref-text tail A/B — no effect; punctuation sanitize — no effect;
  one-sentence chunks — worse; speed 0.9/1.0/1.1 no-trim — zero "गई" but drawl;
  pad 1.05 → 2 fillers, pad 0.95 → 2, pad 0.92 → 2, pad 0.90 → ZERO (boundary
  sharp between 0.90 and 0.92).
- FIX: `external/IndicF5/run_indic.py` `_tail_trim` default `pad: 1.05 → 0.90`
  (+docstring). Verified end-to-end via real `generate()` (merged chunks,
  ellipsis text, speed 0.9, postprocess): 0 "गई" fillers. Trade-off: rare
  clipped final syllable ("है") — 1 per ~7 sentences; pad >0.92 trades it back
  for per-sentence "गई". Upgrade path: word-aligned cut using the worker's
  whisper words.json timestamps.
- **NOTE: `external/IndicF5/` is gitignored — the pad change lives only on this
  machine** (documents/plans + PONYTAIL-DEBT row carry it). The unit test
  `tests/test_indicf5_runner_tail_trim.py` (432 frames now) IS committed and
  runs on CI via the stub loader.
- Ref voice untouched per user lock. `character_voices/narration_ref_9s_mono24k.txt`
  is untracked and ships with this commit (do not delete).

## Commits
- one commit: 14 modified files (image pipeline: panel_compositor, image_gen,
  config_schemas, config.yaml, tests; audio_proxy/indicf5_worker; translation;
  story_planner) + untracked: `character_voices/narration_ref_9s_mono24k.txt`,
  `docs/plans/2026-08-03-hinglish-panel-variety-image-aspect-plan.md`,
  `tests/test_indicf5_runner_tail_trim.py`, PONYTAIL-DEBT.md row.

## Gotchas
- Remote is `github-origin`, NOT `origin`. Push: `git push github-origin HEAD`.
- Always `venv\Scripts\python.exe -X utf8` for Devanagari (cp1252 UnicodeEncodeError otherwise).
- This session's agent cannot view images — frames were extracted to
  `C:\Users\dhruv\AppData\Local\Temp\opencode\vid_5s/30s/60s/90s.png` and PSNR
  compared instead.
- Test matrix wavs kept in Temp\opencode (gen_verify / gen_pad092 / gen_pad095
  / final_t090 …) for re-listening.

## Open items
- Image topic HELD by user: pick A/B/C (see above), then re-run with
  `--images-per-segment N`, re-check frames per beat + whisper-small on new
  audio (pad 0.90 must stay clean), get new verdict.
- If the rare clipped "है" bothers: implement word-aligned trim (whisper
  words.json) — the ledger row's upgrade path.

---

# Session Log — 2026-08-04 (late): dynamic panels per page (dataset variety)

Follow-up on the same day. User: "is 5 image per page are fixed" → yes (two
hardcoded 5s); "can we make it dynamic" → picked **B. Dataset variety**.

## What was done
- Root truth: layouts already supported arbitrary counts per page
  (`plan_page_rects` count param + dataset filter); only the chunking was
  fixed at 5 (`compose_panel_pages` `range(0, len, 5)`) and `_panel_sizes`
  (`divmod(i, 5)`) — the two MUST stay in sync (generation sizes = panel rects).
- Datasets checked: `config/panel_layouts.roboflow.json` 150 layouts
  (counts 1:4, 2:9, 3:10, 4:19, 5:108), fallback 30 layouts (1-5 spread).
  Stored order is scrape order (roboflow_NNN), not curated.
- Added `plan_page_counts(n_images, layout_file, fallback_layout_file,
  default_panels=5)` in `video/image_gen/panel_compositor.py` — walks the
  dataset in order (each page takes the next layout's panel count, cycling;
  final page = first count ≤ remaining, else the remainder via the fixed
  grid). No dataset → legacy uniform 5s. `compose_panel_pages` chunks by it;
  `image_gen._panel_sizes` mirrors it page by page.
- Real-dataset smoke: 25 images → 6 pages `[5,1,5,5,5,4]` (was 3 pages);
  15 → `[5,1,5,4]`; 6 → `[5,1]` (unchanged from before).
- Tests: 5 new in `test_panel_compositor.py` (walk order, cycle+fit, no-fit
  remainder, 5s default, mixed-count compose pixel checks) + 1 sync test in
  `test_image_gen.py` (sizes per page == plan_page_rects of that page).
  60 passed, ruff clean.

## Decisions
- Strict dataset order, no shuffle: deterministic across runs and faithful to
  "next layout in order". The early 1-panel page in the walk = full-page
  splash panel — legitimate manga device, kept.
- No config change needed: `plan_page_counts` derives counts from the same
  layout_file/fallback_layout_file the compositor already uses.

## Commits
- pushed later in this session (single commit with the CI skip fix + this).

## Open items
- Image topic still HELD: re-run with `--images-per-segment N` picks up the
  dynamic pages automatically (25 → 6 pages now, vs 3 before).
- If a page mix with more mid-story variety is wanted later: seed-shuffled
  count sequence — but YAGNI until the re-run shows a problem.

---

# Session Log — 2026-08-04 (evening): word-aligned tail trim (drawl excision)

User: "ok go for a" on **Option A**: forced-alignment trim in
`audio/tts_alignment.py` — whisper-small word timings + true-text DP
alignment, cut drawl-shaped words at segment edge runs. This is the
PONYTAIL-DEBT `_tail_trim` upgrade path (row now marked DONE).

## Design (validated in sim before code)
- `dp_sim3.py` (Temp\opencode) final design: greedy first-match (window=12,
  monotonic pointer), zero-duration junk words EXCLUDED from matching;
  `_raw_dist` = raw edit-distance when either side ≤2 code points (preserves
  कि/के distinctions), skeleton otherwise; cuttable = junk OR (drawl-shaped
  AND not aligned at dist-0); trim only leading/trailing edge runs per whisper
  segment; whole-segment-cuttable → keep all (conservative); mid-segment
  drawls and unaligned real-shaped mishears kept (accepted minor miss:
  39.7s drawl heard as real-shaped "अजहर" kept).
- `_DRAWL_RE = ^ग[ेीईं]?$|^ग{2,}[ेीईं]?$`. Sim result on the real 68.5s wav:
  60 aligned (36 dist-0), 5 cuts (गई/गगे heads+tails) = 1.36s, zero real
  speech loss (visually verified). Final run removed 1.35s — matches.
- Pipeline words.json = flat `[{word, start, end}]`, NO seg field — segments
  are collected from faster-whisper `seg.words` at transcribe time in
  `align_audio` (production code does exactly this).

## What shipped
- `audio/tts_alignment.py`: `_MATRAS`, `_PUNCT`, `_DRAWL_RE`, `_norm`,
  `_skeleton`, `_dist`, `_match_threshold`, `_raw_dist`, `_is_drawl`,
  `_is_junk`, `_align_words`, `_trim_ranges`, `_cut_wav` (soundfile float32,
  complement ranges, temp file + `os.replace`), `_prune_and_remap`;
  `align_audio(..., trim_tails=False)`. Gating: trim only when
  `trim_tails and reference_text and "base" not in model_name and "tiny"
  not in model_name` (base/tiny mask drawls as real words → nothing to cut).
  `_cut_wav` bug found by test: kept the cut ranges instead of the complement
  — fixed with inclusive bounds.
- Config: `config/config.yaml` `tts.alignment: {enabled: true, model: small,
  device: cpu, compute_type: int8, trim_tails: true}`;
  `config/config_schemas.py` `AlignmentConfig.trim_tails: bool = False`
  (schema has `extra: forbid` — first load failed with "Extra inputs are not
  permitted" until the field landed); `audio/audio_proxy.py` passes
  `trim_tails=align_cfg.get("trim_tails", False)`.
- Tests: +6 in `tests/test_tts_alignment.py` (head-drawl cut + remap, dist-0
  drawl kept, mid-segment drawl kept, no-reference skip, base-model skip,
  transcribe language pass). Full suite 1981 passed / 5 skipped, ruff clean.

## Verification (real audio, not just sim)
- `verify_trim.py` on the previous run's real wav (`indicf5_24032b2a.wav`,
  68.52s): trimmed to 67.17s (1.35s = the 5 drawl words), words.json remapped
  to true text; re-whisper of trimmed wav: ZERO गगगग/गगे drawl tokens (was 4
  + heads before).
- Full pipeline re-run (`bootstrap_pipeline.py --topic "The Lantern Maker of
  Old Delhi" --duration 2 --segment-count 1 --words-per-segment 130
  --images-per-segment 15 --run-mode one_time --no-resume --yes`; Ollama was
  down, restarted `ollama serve` first): SUCCESS, 120.2s final video
  `studio_outputs\The_Lantern_Maker_of_Old_Delhi_final_video.mp4`. New audio
  `indicf5_ed262669.wav` (5.50MB) re-whispered with whisper-small: 18
  segments, ZERO drawl tokens; words.json 288 true-text words with only real
  ग-words (गहरे, गई,).

## Gotchas
- verify scripts need `sys.path.insert(0, r'C:\Video.AI')` (run from
  Temp\opencode). `Path.read_text` FileNotFoundError once despite the file
  existing — re-check, don't trust.
- QC "Duration mismatch 120s vs expected 90s" advisory is pre-existing
  behavior (director plan vs actual), not a regression.

## Commits
- To be pushed as one commit: audio/tts_alignment.py, audio/audio_proxy.py,
  config/config.yaml, config/config_schemas.py, tests/test_tts_alignment.py,
  docs/session-2026-08-04.md, PONYTAIL-DEBT.md.

## Open items
- User listens to the new final video → verdict (drawls should be gone; check
  the rare clipped-syllable trade-off is gone with word-aligned cuts).
- Image topic still HELD: `--images-per-segment 25` re-run (6 dynamic pages)
  whenever user wants. TTS re-run automatic (~20 min).

---

# Session Log — 2026-08-04 (night): AGENTS.md rules committed to memory

User: "read agent.md" → "add this all in your memory remember in future when
working on project". Read `AGENTS.md` (159 lines) and internalized all rules
for future project work. Key rules committed to memory:

1. **Ponytail, lazy senior dev mode** — 7-rung ladder before writing code:
   YAGNI → reuse existing → stdlib → native platform → installed dependency →
   one-liner → minimum code that works. Understand the problem first, then climb.
2. **Session memory** — Load `session-memory` skill at start, write/append
   `docs/session-YYYY-MM-DD.md` at end, push to `github-origin` (never `origin`).
3. **Rules** — No unrequested abstractions/dependencies/boilerplate. Deletion
   over addition. Cross-platform paths via `pathlib`/`os.path`. Don't bump
   torch/ComfyUI pins without full test suite. Mark simplifications with
   `ponytail:` comment. Report findings at task end.
4. **Not lazy about** — Understanding the problem, input validation at trust
   boundaries, error handling that prevents data loss, security, accessibility,
   hardware calibration. Non-trivial logic leaves ONE runnable check.
5. **ComfyUI V3** — Tests use `comfy_api.v0_0_2` stubs. Smoke gate:
   `venv\Scripts\python.exe scripts\comfyui_smoke.py`.
6. **CI/Test Deps** — Heavy packages (torch, pyarrow, crewai, faster_whisper,
   whisper) stubbed in CI. Real GPU tests run locally.
7. **Dev Commands** — `python -m pytest tests/test_video_ai_nodes_execution.py -q`,
   `ruff check .`, `mypy --follow-imports=skip --ignore-missing-imports agents/ui_state.py`,
   `cargo test && cargo clippy -- -D warnings && cargo fmt --check`.
8. **Agent Orchestration** — 7 agents (planner, architect, tdd-guide,
   code-reviewer, security-reviewer, python-reviewer, performance-optimizer).
   Proactive invocation. Conflict resolution: surface to user, security wins
   for trust boundaries. Parallel execution for independent ops.
   Multi-perspective analysis with split-role sub-agents.
9. **Rust Worker Crate** — Process supervisor for `bootstrap_pipeline.py`.
   No Torch/ComfyUI deps. Don't modify `bootstrap_pipeline.py`, `core/`,
   `video/`, or SQLite schema. Approved exceptions: PyO3 bridge, Rust audio
   work (opt-in/fallback-safe), `audio/` file touching (interop gates only).
   Mirror constants: heartbeat 10s, cancel grace 30s, poll 5s. If schema
   change needed, STOP and ask. Python interpreter resolves from
   `VIDEOAI_PYTHON` or `venv/Scripts/python.exe` (Windows) / `venv/bin/python`
   (Unix).

The `AGENTS.md` file is automatically loaded as project instructions in every
session, so these rules are available whenever working on this project.

---

# Session Log — 2026-08-04 (night): storyboard-prompt-builder planning (PLANNED, not implemented)

User shared the "storyboard-prompt-builder" framework spec (2-phase: multi-panel
storyboard sheet prompt + cinematic shot-list prompt; character DNA; 8-section
prompt structure) and asked: fit it into the pre-production phase, store
storyboards per story for reuse. Whole session was PLAN MODE — zero code changes.

## What was done (research, read-only)
- Mapped pre-production: `core/pre_production.py run_pre_production` (research →
  vision doc → consultation → writer → overlay → DecisionRecord → memory seed);
  `plan_outline()` at pre_production.py:434 runs once before the segment loop.
- Memory: `memory/project_store.py` — ProjectStore (cross-story chars/visual_locks)
  + StoryStore (`studio_projects/{project}/stories/{story}/story.json` or
  `_one_time/`). Natural home for per-story storyboard.
- Consultation: `agents/director/consultation.py consult_user` — UIState pause
  (300s timeout), `--yes` auto-accept, non-interactive auto-default.
- Image path: segment_runner.py image_node (segment_runner.py:624): build_prompts
  (utils/utils.py:62) → enrich_prompts (utils/scene_director.py:92) →
  generate_images (video/image_gen/image_gen.py:132) → ComfyUI
  (comfyui_client.py generate_image). Panel pages via panel_compositor
  (compose_panel_pages, plan_page_counts — dynamic counts landed earlier today).
- Config: `image_gen.comfyui.reference_usage: direct` (config.yaml:190) already
  resolves master portraits (`_reference_pool` image_gen.py:67-119). Master
  portraits generate LAZILY at first character appearance (pipeline_long.py:426)
  → no character images exist at pre-production time on first runs.
- grep: zero existing "storyboard" code anywhere.

## User decisions (question tool)
1. Scope: Phase 1 + shot-timing metadata (per-panel duration/camera stored,
   optionally consumed by scene_director) — NOT full Phase 2 shot list.
2. Storyboard sheet also feeds back as style reference for scene gen
   (new config `storyboard_reference_usage`, gated off by default).
3. Character refs: text-derived DNA (vision_doc descriptions) + existing
   ProjectStore assets only — NO new image-upload path.

## Plan (approved shape, ready to implement)
- New `core/storyboard.py` run_storyboard(): reuse-check (StoryStore → skip if
  approved unless --force-storyboard) → 1 LLM call (`storyboard_plan` template in
  prompts.yaml) outline beats + DNA + style → structured per-panel JSON (shot
  size/camera/action/environment/dialogue/duration_sec, Wide→Medium→Close-up
  alternation) → panel prompts assembled deterministically → generate via
  existing generate_images → compose ONE 16:9 sheet via existing panel_compositor
  (NO single-prompt grid — SD can't do it reliably) → consult_user approval
  (2 retries, --yes auto) → persist to StoryStore + sheet PNG in story dir.
- Hook in `core/pipeline_long.py` right after plan_outline() (pre-generation;
  run_pre_production untouched).
- Style reference: reuse the master-portrait resolution path in image_gen.py for
  an optional sheet override (gated config).
- Files: core/storyboard.py (NEW), pipeline_long.py, memory/project_store.py
  (save/get_storyboard, JSON only, no schema change), prompts.yaml,
  config/config.yaml + config/config_schemas.py (storyboard section),
  video/image_gen/image_gen.py, utils/scene_director.py,
  bootstrap_pipeline.py (--no-storyboard/--force-storyboard),
  tests/test_storyboard.py (mocked LLM + comfy_api stubs), docs/plans/.

## Decisions
- Single-prompt multi-panel sheet REJECTED — per-panel generation + existing
  compositor reuses everything, no new workflow JSON.
- 8-section prose prompt SLIMMED to structured per-panel JSON + deterministic
  assembly (one LLM call; YAGNI on prose header/footer bloat).
- Text labels/timeline typography on sheet skipped initially (ponytail: upgrade
  path = PIL text draw in compositor if wanted later).

## Open items
- Implement the plan (exit plan mode): storyboard.py first, then hooks/tests.
- Unsettled defaults to confirm: panel_count default (6 vs derived from beats),
  storyboard_reference_usage default (none vs direct).
- Follow-ups from earlier sections still open: Lantern Maker image re-run with
  --images-per-segment (25 → 6 dynamic pages), user verdict on word-aligned trim.

## Gotchas
- Remote is `github-origin`, NOT `origin`.
- `rg` not available in pwsh on this box — use grep tool or Select-String.

## Review note (Cline, 2026-08-04 22:40)
- Reviewed the full storyboard-prompt-builder chat history from the opencode
  session. Session log section above accurately captures: research findings,
  user decisions (Phase 1 + shot-timing, style reference, text-derived DNA),
  plan shape, open items. No gaps found — all 5 sections of today's file cover
  the complete conversation flow across both AI models.
- Plan is APPROVED but NOT IMPLEMENTED. Ready for implementation when user
  gives the go-ahead. Files to touch: `core/storyboard.py` (NEW),
  `core/pipeline_long.py`, `memory/project_store.py`, `prompts.yaml`,
  `config/config.yaml` + `config/config_schemas.py`,
  `video/image_gen/image_gen.py`, `utils/scene_director.py`,
  `bootstrap_pipeline.py`, `tests/test_storyboard.py`.
- Unsettled defaults: `panel_count` (6 vs derived from beats),
  `storyboard_reference_usage` (none vs direct).
- Still-open follow-ups from earlier today: Lantern Maker image re-run with
  `--images-per-segment 25` (6 dynamic pages), user verdict on word-aligned
  trim audio.

---

# Session Log — 2026-08-04 (night): storyboard-prompt-builder IMPLEMENTED

"start implementing plan where it is left" — took over the in-progress
uncommitted storyboard implementation (all 8 modules were already drafted),
verified every real signature, fixed the missing wiring + code-review bugs.

## What was done
- Verified all phase wiring against real code (signatures, StoryStore root
  param, PromptsMixin._prompt KeyError fallback, consult_user strings,
  _panel_sizes geometry chain) — tests/test_storyboard.py (16 tests) + full
  suite 1997 passed / 5 skipped, ruff clean.
- Gap fixed: shot metadata was consumed (enrich_prompts reads
  plan["shot_metadata"]) but NEVER written. The per-segment plan dict IS the
  outline segment (segment_runner.py:828 _build_segment_state) → new
  `attach_shot_metadata(outline, panels)` (core/storyboard.py) round-robins
  panel camera/duration onto outline segments; called from the pipeline hook.
- Code-review agent findings (5 bugs, 4 nits — ALL fixed):
  1. Multi-page sheet: compose_panel_pages chunks [5,1] for 6 panels; only
     pages[0] was kept. Now record keeps `sheet_pages` (all pages), primary
     sheet = pages[0]; nothing silently dropped.
  2. UI-mode "Proceed as planned." (consultation.py:57) failed the "approve"
     check → looped into regenerate. Approval inverted: any non-"regenerate"
     reply approves (advisory gate, never a blocker).
  3. Garbage LLM JSON: extract_json raises ValueError (utils/utils.py:368) →
     now caught in run_storyboard (skip gate) + per-field duration float
     hardened in _parse_panels.
  4. Stale reuse: approved record with deleted sheet PNG was reused → now
     regenerates when Path(sheet_path).is_file() is false.
  5. Hook catch-all (pipeline_long.py) logged warning → now log.error
     "gate NOT applied".
  - Nits: compose geometry now mirrors _panel_sizes (layout_file/
    fallback_layout_file/page_aspect from image_gen.panel_composite — the
    chain that decided generation sizes); pad cap min(panel_count,12) removed
    → exact panel_count; attach_shot_metadata skips zero durations ("0.0s"
    noise); test fakes tightened to real signatures.
- Prompt template (prompts.yaml storyboard_plan) already had a stray
  whitespace edit on story_compaction (indentation +2 spaces on 2 lines) —
  harmless, kept.

## Decisions
- Sheet PNGs land in studio_outputs/{topic}/storyboard/ (consistent with all
  other media) not the story dir; StoryStore record stores the path — reuse
  works regardless. Plan deviation, noted.
- Storyboard compose falls back to sb.aspect (16:9) only when
  panel_composite.enabled is false; otherwise mirrors panel_composite geometry.
- config `aspect` key kept (plan contract) as the fallback page aspect.

## Commits
- To be pushed as one commit: core/storyboard.py (NEW), tests/test_storyboard.py
  (NEW), core/pipeline_long.py, memory/project_store.py, config/config.yaml,
  config/config_schemas.py, prompts.yaml, utils/scene_director.py,
  video/image_gen/image_gen.py, bootstrap_pipeline.py, task_plan.md,
  progress.md, findings.md, docs/session-2026-08-04.md.

## Open items
- Real-instance smoke: one storyboard run via CLI, then --force-storyboard
  re-run to prove reuse-skip (needs Ollama + ComfyUI up).
- Still-open earlier items: Lantern Maker re-run --images-per-segment 25,
  user verdict on word-aligned trim audio.
- Later: beat-title matching upgrade path for attach_shot_metadata
  (round-robin is the ponytail fallback).

---

# Session Log — 2026-08-04 (night): storyboard refinements — dynamic panel count + sheet-as-reference after first run

User: "pannel count dynamic and refrence ussage after first run its store artifact are reference".

## What was done
- `_dynamic_panel_count(outline, fallback, cap=12)` in core/storyboard.py: panel
  count now derives from the outline = sum of seg num_images (one panel per
  planned scene image), capped at 12 (~3 sheet pages), falling back to config
  panel_count when segments lack num_images. ponytail: cap 12, upgrade path =
  config field.
- Style reference: the hook in core/pipeline_long.py now wires
  image_gen.comfyui.storyboard_sheet whenever a sheet exists (fresh OR reused)
  — dropped the `reference_usage == "direct"` gate. After the first run the
  stored approved sheet IS the reference image for scene gen.
- `reference_usage` field deleted from StoryboardConfig + config.yaml (dead
  config after the gate removal; added this session, never shipped).
- Tests: +3 (dynamic from outline 3+2=5, cap 8+8→12, fallback to config 2);
  fake LLM now records prompts; fake storyboard_plan template mirrors the real
  "exactly {panel_count} panels" wording. 2000 passed / 5 skipped, ruff clean.

## Commits
- second commit this session (storyboard refinements), pushed to github-origin.

## Open items
- Real-instance smoke still pending: one CLI run then --force-storyboard re-run.
- Lantern Maker re-run --images-per-segment 25 (now yields a 12-panel
  storyboard preview on fresh runs), word-aligned trim audio verdict.

---

# Session Log — 2026-08-04 (night): storyboard bug hunt (static, no real run)

User asked to find/fix bugs via a real run, then revoked it ("no real run
allowed") — did a static end-to-end trace instead; unit suite for verification.

## Bugs found + fixed
1. `video/image_gen/image_gen.py` `_stable_character_reference`: the sheet
   check sat BEHIND the `reference_usage != "direct"` early return — with
   reference_usage set to the schema default `style_inspiration` the stored
   sheet would silently stop being the reference, contradicting the user's
   "after first run its store artifact is the reference". Sheet check now
   precedes the gate. (Real config.yaml has reference_usage: direct, so it
   masked the bug on default config.)
2. `core/storyboard.py` `_build_llm_prompt` fallback prompt: literal doubled
   braces `{{"panels"` in plain (non-f) strings — the LLM was shown invalid
   JSON syntax in the fallback prompt. Single braces now.
3. `core/storyboard.py` compose geometry: layout_file/fallback_layout_file
   passed None when unset, while `_panel_sizes` defaults to
   config/panel_layouts.json — dataset-walk geometry drift. Now mirrors the
   schema defaults exactly.
4. `core/storyboard.py` primary sheet was always pages[0] — the roboflow
   dataset walk can lead with a 1-panel splash layout, making the approval
   target a single panel. New `_primary_sheet_page()` picks the page with the
   most panels (same plan_page_counts chain); extras stay in the record.

## Tests
+3: fallback prompt single braces; primary sheet = fullest page (2 pages,
picks storyboard_2.png); sheet beats the reference_usage gate (test_image_gen).
Full suite 2003 passed / 5 skipped, ruff clean.

## Commits
- third commit this session (storyboard bug fixes), pushed to github-origin.

## Open items
- Real-instance smoke STILL pending (user forbade real runs this session):
  one CLI run then --force-storyboard re-run; watch that the primary sheet is
  the fullest page on the robofield walk.
