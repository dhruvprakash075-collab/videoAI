# Face Reference Pools

Local face reference images go here. Git keeps this README, not the images.

The current default config uses:

```text
reference_assets/face_reference_pools/anime_face/
```

The images are used as IPAdapter visual references. They improve anime-face
style and rough consistency, but they are not the same as training a character
LoRA.

Do not commit downloaded dataset images or model files.
